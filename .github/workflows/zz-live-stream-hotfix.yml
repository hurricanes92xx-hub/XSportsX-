name: Legacy live sports hotfix
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy hotfix workflow retained for manual use; live stream matching is handled by the EPG resolver workflow."
