{
  "name": "xsportsx-nuvio",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "XSportsX — futuristic, multi-source live sports hub for Nuvio.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check core.js && node --check providers.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
