{
  "name": "xsportsx-nuvio",
  "version": "5.0.25",
  "private": true,
  "type": "module",
  "description": "XSportsX — fast league-isolated sports command center with Xtream sports-news and live streams.",
  "engines": { "node": ">=20.19.5" },
  "scripts": {
    "start": "node render-entry.js",
    "dev": "BASE_URL=http://localhost:7000 node --watch render-entry.js",
    "test": "node --test",
    "check": "node --check render-proxy.js"
  },
  "dependencies": { "express": "^5.1.0" }
}
