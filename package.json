{
  "name": "xsportsx-nuvio",
  "version": "3.7.0",
  "private": true,
  "type": "module",
  "description": "XSportsX — cinematic, multi-source live sports hub for Nuvio with a premium UFC command center.",
  "engines": {
    "node": "20.19.5"
  },
  "scripts": {
    "start": "node gateway.js",
    "dev": "node --watch gateway.js",
    "test": "node --test",
    "check": "node --check server.js && node --check server-fixed.js && node --check gateway.js && node --check core.js && node --check providers.js && node --check providers-core.js && node --check free-sources.js && node --check ufc-data.js && node --check visuals.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
