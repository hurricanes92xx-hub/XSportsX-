{
  "name": "xsportsx-nuvio",
  "version": "3.3.3",
  "private": true,
  "type": "module",
  "description": "XSportsX — cinematic, multi-source live sports hub for Nuvio.",
  "engines": {
    "node": "20.19.5"
  },
  "scripts": {
    "start": "node server-fixed.js",
    "dev": "node --watch server-fixed.js",
    "test": "node --test",
    "check": "node --check server-fixed.js && node --check core.js && node --check providers.js && node --check visuals.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
