// Stable boot shim.
// Keep startup side-effect free so Render can boot the production gateway.
// Source discovery is handled by the gateway's normal source-manager path.
console.log("[XSportsX] stable boot shim loaded");
