// Stable boot shim.
// Source-resolution experiments are disabled here so they cannot prevent the
// Nuvio gateway/catalog layer from starting. The production gateway owns the
// single sports-router process.
console.log("[XSportsX] stable boot shim loaded");
