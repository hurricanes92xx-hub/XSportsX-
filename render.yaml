services:
  - type: web
    name: xsportsx
    runtime: node
    plan: free
    autoDeploy: true
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: XSPORTSX_CONFIG_SECRET
        sync: false
